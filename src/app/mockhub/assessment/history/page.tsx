'use client';

import { AssessmentEngine } from '@/components/mocks/components/AssessmentEngine';

export default function AssessmentHistoryPage() {
  return (
    <div className="w-full animate-in fade-in zoom-in-95 duration-500">
      <AssessmentEngine />
    </div>
  );
}
