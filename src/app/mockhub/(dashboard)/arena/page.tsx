'use client';

import { MockArena } from '@/components/mocks/components/MockArena';

export default function ArenaPage() {
  return (
    <div className="w-full animate-in fade-in zoom-in-95 duration-500">
      <MockArena />
    </div>
  );
}
